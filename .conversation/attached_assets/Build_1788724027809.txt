You are an expert full-stack engineer, Telegram Bot API developer, and Google Gemini API integration specialist.
I want you to BUILD a complete, production-ready Telegram AI assistant inside this Replit project.
I already have:
1. A Telegram Bot Token
2. A Google Gemini API Key
DO NOT ask me to paste either secret into source code.
Use Replit Secrets / environment variables for all credentials.
The final application must allow me to open Telegram, message my bot naturally, and receive intelligent responses generated by Google Gemini.
================================================== CORE OBJECTIVE
Build this system:
Telegram User ↓ Telegram Bot ↓ Node.js/TypeScript Backend ↓ Google Gemini API ↓ AI Response ↓ Telegram User
The bot should feel like a personal AI assistant, not a basic command-response bot.
================================================== TECHNOLOGY
Use:
• Node.js
• TypeScript
• Telegram Bot API
• grammY (preferred) or another well-maintained Telegram framework if you have a strong technical reason
• Official Google Gemini SDK / Google GenAI SDK
• PostgreSQL for persistent conversation storage
• Express or Fastify if needed for HTTP/webhook handling
• npm
Use modern, maintained packages.
Before implementing Gemini functionality, verify that the SDK methods and package names you use are currently supported. Do not invent API methods.
Use TypeScript strict mode.
================================================== REPLIT REQUIREMENTS
This project will initially run inside Replit.
Use Replit Secrets for:
TELEGRAM_BOT_TOKEN GEMINI_API_KEY DATABASE_URL GEMINI_MODEL ALLOWED_TELEGRAM_USER_IDS PORT
Create a .env.example containing variable names only.
Never commit secrets.
Create a proper .gitignore.
Do not print secrets in logs.
================================================== FIRST: INSPECT THE REPLIT PROJECT
Before changing anything:
1. Inspect the existing files.
2. Determine whether this is an empty project or an existing application.
3. Do not unnecessarily delete existing useful files.
4. If an existing package.json exists, inspect it before modifying dependencies.
5. Reuse existing infrastructure where appropriate.
6. Explain briefly what you found.
7. Then implement the bot.
If the project is empty, initialize the required structure yourself.
================================================== BOT FEATURES
Implement:
/start /help /clear /reset /status
/start
Send a friendly welcome message explaining that the user can simply type a message to chat with the AI.
/help
Show available commands and explain basic usage.
/clear
Clear the user's current conversation history.
/reset
Completely reset the user's current AI session/context.
/status
Show basic non-sensitive status information:
• Bot status
• Gemini status
• Current Gemini model
• Memory status
Never reveal:
• API keys
• Database credentials
• Internal server information
================================================== NORMAL CHAT
Any normal text message should be sent to Gemini.
Example:
User: Hello
Bot: Hello! How can I help you today?
User: My name is David.
Bot: Nice to meet you, David.
User: What is my name?
Bot: Your name is David.
The bot must maintain conversation context.
================================================== CONVERSATION MEMORY
Implement persistent conversation history using PostgreSQL.
Each Telegram user/chat must have isolated conversation history.
User A must NEVER receive User B's context.
Store at minimum:
users
• id
• telegram_user_id
• username
• first_name
• last_name
• created_at
• updated_at
conversations
• id
• telegram_user_id
• chat_id
• created_at
• updated_at
messages
• id
• conversation_id
• role
• content
• created_at
Add appropriate indexes.
Use parameterized queries.
Do not construct SQL using unsafe string concatenation.
================================================== CONTEXT MANAGEMENT
Do NOT send unlimited conversation history to Gemini.
Implement a configurable history limit.
For example:
MAX_HISTORY_MESSAGES
If the conversation becomes very long, only send the most recent relevant messages to Gemini.
Keep system instructions separate from user conversation history.
Make the context limit configurable.
================================================== AI PERSONALITY
Create a separate configuration for the Gemini system instruction.
Default:
"You are a helpful, intelligent, precise, and reliable personal AI assistant. Answer naturally and clearly. Give practical explanations. Do not invent facts. If you are uncertain, say so. When a question requires current information that you cannot verify, clearly state that limitation."
Do not hardcode this deeply inside the Telegram handlers.
Make it easy to change later.
================================================== GEMINI SERVICE
Create a dedicated Gemini service.
The service must:
• Initialize the official Google Gemini SDK.
• Use GEMINI_API_KEY.
• Use GEMINI_MODEL.
• Accept conversation history.
• Send the current user message.
• Return the generated response.
• Handle API failures.
• Handle rate limits.
• Handle timeouts.
• Handle malformed responses.
• Never expose the API key.
Do not place Gemini API calls directly inside the Telegram handler.
Use a clean service architecture.
================================================== TELEGRAM SERVICE
Keep Telegram-specific logic separate from Gemini logic.
Telegram handlers should:
1. Receive message.
2. Validate/authorize user.
3. Save user/message.
4. Retrieve appropriate conversation context.
5. Call Gemini service.
6. Save Gemini response.
7. Send response to Telegram.
================================================== LONG RESPONSES
Telegram has message-length limitations.
If Gemini returns a response longer than Telegram allows:
• Split it into multiple messages.
• Preserve formatting where possible.
• Avoid breaking Markdown/code blocks unnecessarily.
• Send chunks sequentially.
Create a reusable message-splitting utility.
Write tests for it.
================================================== TYPING INDICATOR
While Gemini is generating a response, show Telegram's typing indicator.
If generation takes longer than the Telegram indicator duration, refresh it appropriately.
The user should know that the bot is processing the request.
================================================== USER AUTHORIZATION
Implement an optional Telegram user allowlist.
Environment variable:
ALLOWED_TELEGRAM_USER_IDS
Support comma-separated Telegram user IDs.
Example:
ALLOWED_TELEGRAM_USER_IDS=123456789,987654321
Behavior:
If the variable is empty: Allow all users.
If IDs are configured: Only those Telegram user IDs may use the bot.
Unauthorized users should receive:
"Sorry, this bot is currently private."
Do not reveal the configured user IDs.
================================================== RATE LIMITING
Implement per-user rate limiting.
Prevent a single Telegram user from flooding the bot with requests.
Make the values configurable.
When a user exceeds the limit, send a friendly response explaining that they should wait before sending another request.
Do not crash.
================================================== ERROR HANDLING
Handle:
• Invalid Telegram token
• Invalid Gemini API key
• Gemini rate limits
• Gemini API outage
• Network failures
• Database failures
• Telegram API failures
• Empty messages
• Timeouts
• Unexpected exceptions
Users should receive a friendly generic error.
Server logs should contain enough technical information for debugging.
Never expose:
• API keys
• Database passwords
• stack traces
• internal infrastructure
• environment variables
to Telegram users.
================================================== LOGGING
Implement useful server-side logging.
Log:
• Startup
• Shutdown
• Database connection
• Telegram initialization
• Gemini errors
• Database errors
• Unexpected exceptions
Do NOT log secrets.
Avoid logging complete private conversations unless absolutely necessary for debugging.
================================================== HEALTH CHECK
Create:
GET /health
Return:
{ "status": "ok" }
Do not expose credentials or sensitive infrastructure information.
================================================== WEBHOOK / TELEGRAM UPDATES
Choose the most reliable architecture for Replit.
If the Replit deployment provides a stable public HTTPS URL, use Telegram webhooks.
Create the necessary webhook endpoint.
Make the public URL configurable.
If polling is technically more appropriate for the current Replit environment, explain why before choosing it.
Do not implement unnecessary duplicate update mechanisms.
Ensure that only one Telegram update mechanism is active at a time.
================================================== DATABASE
Use PostgreSQL.
Create:
• Database connection module
• Schema/migrations
• Repository/data-access layer
• Proper indexes
• Connection cleanup
Do not assume a local database exists.
Use DATABASE_URL.
The application should fail clearly if DATABASE_URL is missing when persistent memory is required.
================================================== PROJECT STRUCTURE
Use a clean structure such as:
src/ config/ env.ts ai.ts
bot/ bot.ts handlers.ts middleware.ts
gemini/ gemini.service.ts
database/ db.ts schema/ repositories/
services/ conversation.service.ts authorization.service.ts rate-limit.service.ts
utils/ split-message.ts logger.ts
server.ts index.ts
tests/
Adjust this structure if you have a better architecture.
Do not create unnecessary complexity.
================================================== TESTING
Create automated tests for:
1. Environment validation
2. User authorization
3. Conversation isolation
4. Message splitting
5. Rate limiting
6. Health endpoint
7. Gemini service behavior using mocks
Do not make tests depend on the real Gemini API.
Do not make tests depend on the real Telegram API.
================================================== TYPE SAFETY
Use strict TypeScript.
Run:
• TypeScript compiler
• Tests
• Linter if configured
Fix all errors before declaring the project complete.
Do not suppress errors with:
any
unless there is a documented technical reason.
================================================== REPLIT RUN CONFIGURATION
Configure the project so Replit can start it using a simple command such as:
npm run dev
for development and:
npm run build npm start
for production.
Ensure PORT comes from the environment.
Do not hardcode the port.
================================================== GITHUB COMPATIBILITY
The project must be cleanly pushable to GitHub.
Include:
.gitignore README.md .env.example
Never include:
.env API keys Telegram tokens database passwords
================================================== README
Create a complete README containing:
1. Project overview
2. Architecture
3. Features
4. Requirements
5. Replit setup
6. Replit Secrets setup
7. Telegram BotFather configuration
8. Gemini API configuration
9. PostgreSQL configuration
10. Local development
11. Testing
12. GitHub setup
13. Deployment
14. Webhook configuration
15. How to obtain Telegram user ID
16. How to restrict bot access
17. How to change Gemini model
18. How to modify AI personality
19. Troubleshooting
================================================== PRODUCTION READINESS AUDIT
After implementation, perform a self-audit.
Check:
SECURITY
• No hardcoded secrets
• Secrets only come from environment variables
• No secret logging
• User isolation
• SQL injection protection
• Authorization
TELEGRAM
• Correct API usage
• Message-length handling
• Error handling
• Typing indicator
• Webhook/polling correctness
GEMINI
• Current official SDK
• Correct API usage
• Correct conversation format
• Error handling
• Rate limits
• Context limits
DATABASE
• Correct schema
• Correct indexes
• Connection handling
• Conversation isolation
REPLIT
• Correct startup command
• Correct PORT handling
• Secrets compatibility
• Public HTTPS/webhook compatibility
CODE
• TypeScript errors fixed
• Tests passing
• No placeholder core functionality
• No unnecessary complexity
================================================== IMPORTANT EXECUTION RULE
Do not just give me instructions.
Actually create and modify the files in this Replit project.
Install required dependencies.
Run the application.
Run the tests.
Run TypeScript checks.
If you encounter errors:
1. Diagnose them.
2. Fix them.
3. Run the checks again.
4. Continue until the project is working.
Do not stop at the first error.
================================================== FINAL RESPONSE
When the implementation is complete, give me:
1. What you built
2. Project structure
3. Dependencies installed
4. Replit Secrets I need to configure
5. Exact commands to run
6. How to test the Telegram bot
7. How to get my Telegram user ID
8. How to restrict the bot to only me
9. How to connect PostgreSQL
10. How to deploy it
11. Any remaining configuration I must perform manually
IMPORTANT:
Do not ask me for my actual Telegram Bot Token or Gemini API Key in chat.
Tell me exactly where to put them in Replit Secrets instead.
Build the actual application now.